#include <time.h>
#include <stdio.h>

/**
 * main - print positive or negative
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i, int i++;
	for (i = 0 ; i <= 10 ; i++)
	printf("%d\n", i);
return (0);

}

