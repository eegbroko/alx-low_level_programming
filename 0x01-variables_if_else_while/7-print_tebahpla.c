#include <time.h>
#include <stdio.h>

/**
 * main - print 7-print_tebahpla
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 122; i >= 97; i--)
	{
		if (i >= 97)
		{
			putchar(i);
		}
	}
	putchar('\n');
	return (0);
}

